\documentclass{article}

\usepackage[dblblindworkshop]{neurips_2026}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{url}
\urlstyle{same}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{microtype}
\usepackage{xcolor}
\renewcommand{\ttdefault}{pcr}

\newtheorem{proposition}{Proposition}
\newcommand{\zc}{Z_c}
\newcommand{\ze}{Z_e}
\newcommand{\Dcand}{D_{\mathrm{cand}}}
\newcommand{\RPmax}{R_{\mathrm{P1\text{-}max}}}

% Central empirical results. The post-run agent should update these commands
% from generated result artifacts rather than editing numeric claims in prose.
\newcommand{\SplitCells}{60}
\newcommand{\SplitBlocks}{12}
\newcommand{\SplitPairs}{300}
\newcommand{\SplitAnalyzedCells}{59}
\newcommand{\SplitAnalyzedPairs}{289}
\newcommand{\SplitScoreNullPairs}{11}
\newcommand{\SplitMatched}{0.911}
\newcommand{\SplitIndependent}{0.586}
\newcommand{\SplitGap}{0.325}
\newcommand{\SplitCI}{$[0.193, 0.467]$}
\newcommand{\SplitP}{0.000977}
\newcommand{\FGSMArchivedMatched}{1.000}
\newcommand{\FGSMArchivedSplit}{0.985}
\newcommand{\FGSMCeilingExit}{0.25}
\newcommand{\PGDArchivedMatched}{1.000}
\newcommand{\PGDArchivedSplit}{0.985}
\newcommand{\PGDCeilingExit}{0.25}
\newcommand{\ConstructCell}{Qwen2.5-1.5B / SST-2 / layer 14}
\newcommand{\ConstructCandidates}{60}
\newcommand{\ConstructRawMedian}{0.170}
\newcommand{\ConstructOrientedMedian}{0.170}
\newcommand{\ConstructFreshLinear}{0.826}
\newcommand{\ConstructFreshMLP}{0.833}
\newcommand{\ConstructFreshLinearBaseline}{0.856}
\newcommand{\ConstructFreshMLPBaseline}{0.854}
\newcommand{\BugFilteredN}{2{,}000}
\newcommand{\BugFilteredMedian}{0.998}
\newcommand{\BugFilteredCeiling}{26\%}
\newcommand{\BugFilteredAlterRep}{72\%}

\title{Evaluator Reuse Inflates Intervention Scores: A Validity Audit of Probe-Ranking Targets}
\workshoptitle{Linguistic Principles for Foundation Models}
\author{Anonymous Authors}

\begin{document}

\maketitle

\begin{abstract}
Intervention-based rankings of probes for linguistic structure can mistake damage to a downstream decoder for evidence that a candidate probe found a causally important direction. We audit a six-family foundation-model pipeline spanning subject--verb agreement, sentiment, and gender agreement, in which the decoder both supplies an edit and scores it while the reported target is constant across candidates. In a registered matched-versus-split control over 60 requested model--layer--task cells (59 analyzable) nested in 12 model--task blocks, AlterRep target damage falls from \SplitMatched{} under the generating decoder to \SplitIndependent{} under a separately trained evaluator (gap \SplitGap{}, 95\% hierarchical block-bootstrap interval \SplitCI{}; exact block sign-flip $p=\SplitP$). After excluding all records affected by three implementation bugs, \BugFilteredN{} records remain: the cell score has median \BugFilteredMedian{}, \BugFilteredCeiling{} are exactly $1.0$, and AlterRep is the first maximizer in \BugFilteredAlterRep{}. We derive a five-part scored-object validity protocol and run prespecified perturbation-budget, orientation, and fresh-decoder checks. Fixed-decoder damage alone is not evidence of concept erasure or probe reliability.
\end{abstract}

\section{Introduction}

Probes are widely used to infer whether foundation models encode linguistic structure. A probe-ranking target is useful only if it measures the candidate being ranked; in causal probing, a score can instead describe the edit, evaluator, or representation. We trace those dependencies before interpreting correlations with probe properties.

Our central control isolates one such failure mode. AlterRep uses a decoder-derived direction to edit a representation. When the same decoder also scores that edit, mean target damage is \SplitMatched{}. Replacing it with a separately trained split evaluator reduces mean damage to \SplitIndependent{}, an available-case gap of \SplitGap{} across 59 analyzable cells from 60 requested model--layer--task cells nested in 12 model--task blocks. The 95\% hierarchical block-bootstrap interval is \SplitCI{}, and the primary exact two-sided block sign-flip test gives $p=\SplitP$. The edit is unchanged; only the scoring decoder changes.

Three bugs affected model states, labels, or candidate duplication: a padding error collapsed Gemma states, WinoGender preprocessing removed the disambiguating pronoun, and a no-gradient kernel duplicated two MLP variants. Removing every affected Phase-1 row leaves the abstract's bug-filtered result unchanged. The benchmark is an internal, unpublished adaptation of \citet{canby2025reliable}; unlike their intervention-level framework, it reuses fixed probes, maximizes across methods, and copies one cell score to every candidate. We audit only this adaptation.

Linear probes show decodability, not model use \citep{alain2017understanding,hewitt2019designing,belinkov2022probing}. Interventions such as INLP, RLACE, amnesic probing, and LEACE add evidence under method-specific assumptions \citep{ravfogel2020null,ravfogel2022linear,elazar2021amnesic,belrose2023leace}; probe-derived directions can also mix concept and non-concept features \citep{kumar2022unreliable}. Our contribution is the falsifiable protocol in Table~\ref{tab:protocol}: trace candidate identity, separate editing from evaluation, test inversion and redecoding, separate examples, and cluster inference at independent units.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{0.24\linewidth}p{0.33\linewidth}p{0.34\linewidth}}
\toprule
Requirement & Falsifying check & Required response \\
\midrule
Candidate identity & Trace $T(c,g)$; test variance within $g$ & Derive the edit or estimate from $c$ \\
Evaluator separation & Compare matched and split scoring & Train the evaluator on separate data \\
Construct validity & Test inversion, saturation, and fresh decoders & Report damage and redecodability separately \\
Data separation & Audit direction-fit and scoring examples & Cross-fit direction estimation and scoring \\
Inferential support & Map shared data, exclusions, and clusters & Resample independent units; publish exclusions \\
\bottomrule
\end{tabular}
\caption{Scored-object validity protocol. Each row states a falsifying check and the minimum design response.}
\label{tab:protocol}
\end{table}

\section{Evaluator reuse changes measured damage}
\label{sec:split}

The matched-versus-split control holds each edited tensor fixed and changes only the evaluator. For each of six models, we use 5 prespecified sampled layers on SVA and SST-2. Five independently seeded attacker/evaluator pairs train on sentence-disjoint examples. The attacker generates each edit, and both conditions score its identical tensor. We average the five pair contrasts within each model--layer--task cell and use the 12 model--task blocks as the independent units for primary inference (300 requested pair units total). The locked denominator floor yielded 11 affected pair units; all 300 requested pairs remain in the row artifact, while both conditions of each affected pair are excluded symmetrically, leaving 289 pairs and 59 analyzable cells in the available-case estimand. 1 requested cell is explicitly non-estimable.

Across 59 analyzable, equally weighted model--layer--task cells from 60 requested cells, AlterRep mean target damage drops from \SplitMatched{} to \SplitIndependent{}. The mean paired gap is \SplitGap{}, with a 95\% hierarchical block-bootstrap interval of \SplitCI{} and primary exact two-sided block sign-flip $p=\SplitP$. Because both conditions score the identical edited tensor, the gap identifies evaluator reuse as a source of measured damage in this pipeline. The estimate uses 289 complete pairs; the 11 denominator-floor pairs remain explicit in the row artifact with both conditions excluded symmetrically.

% BEGIN POST-RUN FLOOR-SENSITIVITY UPDATE
\paragraph{Denominator-floor robustness.}
The registered available-case estimand remains primary. A separately labeled post-hoc analysis reports the full-case raw accuracy-drop contrast, every prespecified denominator floor, and a deterministic partial-identification bound at the locked floor.
% END POST-RUN FLOOR-SENSITIVITY UPDATE

% BEGIN POST-RUN EPSILON-SWEEP UPDATE
\paragraph{Perturbation-budget sweep.}
At the archived $\epsilon=0.5$ budget, FGSM has matched and split damage \FGSMArchivedMatched{} and \FGSMArchivedSplit{}, while PGD has \PGDArchivedMatched{} and \PGDArchivedSplit{}. The prespecified curves do not jointly show an archived-budget ceiling and split-evaluator damage leaving that ceiling at a lower budget. The sweep therefore does not support attributing the archived saturation to perturbation magnitude alone. No large nonmonotonic reversal exceeds the predeclared 0.100 threshold; this diagnostic is reported separately from the perturbation-budget interpretation. The locked denominator floor produced 260 null score rows across 220 paired epsilon units; both scoring conditions of every affected unit remain in the row artifact and are excluded symmetrically from curve estimates. This makes 40 requested curve cells non-estimable; all model--task blocks remain represented.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_epsilon_sweep.pdf}
  \caption{FGSM and PGD over the complete prespecified $\epsilon$ grid. Curves show equal-cell mean target damage with 95\% model--task cluster intervals; the companion rows show the fraction of pair units at $C=1$. The dotted vertical line marks the archived $\epsilon=0.5$ setting, and zero is the separately plotted no-op point.}
  \label{fig:epsilon-sweep}
\end{figure}
% END POST-RUN EPSILON-SWEEP UPDATE

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_circularity_expanded.pdf}
  \caption{Matched-versus-split AlterRep target damage for 59 analyzable cells from 60 requested model--layer--task cells nested in 12 model--task blocks. Panel A shows paired cell means for the identical edits; the grand matched and split means are \SplitMatched{} and \SplitIndependent{}. Panel B shows all block depth trajectories and the equal-block mean gap with a 95\% hierarchical interval. Denominator-floor nulls affect 11 of 300 requested pair units and are excluded symmetrically.}
  \label{fig:coupling}
\end{figure}

\section{The audited target cannot rank candidates}
\label{sec:audited-target}

\paragraph{Benchmark and provenance.}
The internal benchmark crosses six transformer families, SVA \citep{linzen2016assessing}, SST-2 \citep{socher2013recursive}, and WinoGender \citep{rudinger2018gender}, five layers, three probe architectures, and 20 seeds. Models are BERT, GPT-2, Pythia, Qwen, Gemma, and Llama \citep{devlin2019bert,radford2019language,biderman2023pythia,qwen2024qwen25,gemmateam2024gemma2,grattafiori2024llama}. A preregistered Hessian association failed; we then registered the computation-graph audit, matched-versus-split control, and candidate-conditioned diagnostic. Appendix~\ref{app:design} gives folds, gates, exclusions, and inference units.

\paragraph{The audited score.}
Phase 1 trains fixed linear target and control evaluators for each cell. Let $a^{Z_c}_{\mathrm{pre}}$ and $a^{Z_c}_{\mathrm{post},m}$ denote target-evaluator accuracy before and after edit $m$; define $a^{Z_e}$ analogously for the control evaluator. The pipeline records
\begin{align}
\widetilde C_m&=\operatorname{clip}\!\left(\frac{a^{Z_c}_{\mathrm{pre}}-a^{Z_c}_{\mathrm{post},m}}{a^{Z_c}_{\mathrm{pre}}-0.5},0,1\right), &
\widetilde S_m&=\operatorname{clip}\!\left(\frac{a^{Z_e}_{\mathrm{post},m}-0.5}{a^{Z_e}_{\mathrm{pre}}-0.5},0,1\right),
\end{align}
then computes $H(C,S)=2CS/(C+S)$ when $C+S>0$, with $H(0,0)=0$, and
\begin{equation}
\label{eq:phase1}
\RPmax(g)=\max_{m\in\{\mathrm{INLP,RLACE,AlterRep,FGSM,PGD}\}}H(\widetilde C_m,\widetilde S_m).
\end{equation}
Neither the accuracy ratios nor the maximum in Eq.~\ref{eq:phase1} comes from \citet{canby2025reliable}. Further, $\widetilde C$ measures damage to one fixed decoder. It does not certify information removal.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_pipeline.pdf}
  \caption{The audited adaptation repeats one target within a cell (left). The candidate-conditioned diagnostic derives an edit from each candidate and uses separately trained evaluators (right), but its direction estimation and scoring still share examples.}
  \label{fig:pipeline}
\end{figure}

\paragraph{Candidate independence.}
The pipeline copies $\RPmax(g)$ to all 60 candidates in cell $g$, so each cell has one target value despite candidate test-accuracy ranges of $0.46$--$0.94$ on SVA and $0.50$--$0.89$ on SST-2. Pooled correlations repeat a cell outcome across architectures and seeds; they cannot identify a within-cell ranking.

\begin{proposition}[Cell-constant targets]
\label{prop:constant}
Let $\mathcal G$ be a finite set of cells and let each $\mathcal C_g$ be a finite, nonempty candidate set. If $T(c,g)=t_g$ for every $g\in\mathcal G$ and $c\in\mathcal C_g$, then $T$ induces a complete tie within each cell. Any pooled Spearman association between a candidate statistic $X$ and $T$ contains only between-cell information.
\end{proposition}

The proof and weighting consequences appear in Appendix~\ref{app:proof}.

\paragraph{Aggregation and orientation.}
Across 5,400 records, 48.9\% equal $1.0$ and the median is $0.9999$; replacing the maximum by a mean removes much of this ceiling but is not a validated substitute. The structural failure survives the bug-filtered analysis. The accuracy ratio also confuses inversion with loss: a balanced decoder can move from perfect to zero accuracy while complemented predictions remain perfect. Calibration-frozen orientation metrics and fresh decoders distinguish these cases.

\section{A scored-object validity protocol}
\label{sec:protocol}

Table~\ref{tab:protocol} turns the observed failure modes into checks that can reject a ranking target before any correlation analysis. Passing every row removes the channels listed here; it does not prove that an edit captures the intended construct.

% BEGIN POST-RUN CONSTRUCT-CONTEXT UPDATE
We ran the candidate-identity, evaluator-separation, saturation, data-overlap, and dependence-aware inference checks. The registered archive lacked per-example artifacts for retrospective orientation and fresh-decoder tests; a disclosed pilot and a separately registered prospective panel provide those follow-up checks without changing the original analysis.
% END POST-RUN CONSTRUCT-CONTEXT UPDATE

% BEGIN POST-RUN CONSTRUCT-PANEL UPDATE
% BEGIN POST-RUN ORIENTATION-REDECODABILITY UPDATE
\paragraph{Construct follow-up.}
The disclosed pilot compatibility endpoints remain in Appendix~\ref{app:pilot-compatibility}; the prospective panel replaces this placeholder after its exact-key validation gate passes.
% END POST-RUN ORIENTATION-REDECODABILITY UPDATE
% END POST-RUN CONSTRUCT-PANEL UPDATE

\paragraph{Candidate-conditioned diagnostic.}
$\Dcand$ projects the leading right-singular direction of each candidate's logit-difference Jacobian and scores the edit with five separately trained evaluators. This repairs candidate identity, maximum aggregation, and direct evaluator reuse, but not every validity condition. It recovers planted rankings in linear and held-out nonlinear synthetic families, yet 93.3\% of nonlinear scores lie within $0.02$ of one, above the registered 20\% saturation limit; the locked battery therefore rejects it. The real-data Hessian association ($\rho=-0.042$) remains descriptive.

\section{Scope, responsible use, and conclusion}

The scope is narrow: the benchmark is internal and unpublished, WinoGender is small and flawed, 34 of 90 Phase-2 cells are excluded, model--task data are reused across layers, and no downstream language-model behavior is measured. The candidate diagnostic removes one direction, protects one control, and fails its nonlinear synthetic gate. These facts rule out claims about erasure, prevalence, or Hessian geometry in general.

% BEGIN POST-RUN CONSTRUCT-SCOPE UPDATE
The original archives still lack Phase-2 per-example predictions, edited representations for fresh-decoder training, failed layer-evaluator accuracies, and Hessian eigenvectors. New extension artifacts support the disclosed pilot and prospective middle-layer construct panel, but do not repair the historical archive or turn task-decoder recovery into evidence about downstream model behavior. The finite decoder battery remains a falsification check and cannot establish erasure.
% END POST-RUN CONSTRUCT-SCOPE UPDATE

Invalid scores can support false claims about concept removal, bias mitigation, or safety. Here, the same AlterRep edit appears nearly complete under its generating decoder and substantially weaker under a split evaluator, invalidating the score as a probe-quality target. Similar claims should first trace the scored object, separate editing from evaluation, test inversion and redecodability, and align inference with shared data.

The positive impact is narrower but practical: scored-object audits can prevent linguistic probing results from being overstated as evidence about model use, removal, or safety. The main risk is that readers treat evaluator separation or threshold passing as a universal certificate of erasure. We mitigate that risk by treating the decoder battery as a falsification test, requiring independent evaluators and construct checks, and making no claim about downstream model behavior.

\clearpage
\bibliographystyle{plainnat}
\bibliography{references}

\clearpage
\appendix

\section{Complete proof of Proposition~\ref{prop:constant}}
\label{app:proof}

Fix a finite set of cells $g$ and candidates $c\in\mathcal C_g$. Assume $T(c,g)=t_g$. For any two candidates $c_1,c_2\in\mathcal C_g$, $T(c_1,g)=T(c_2,g)$, so the target induces a complete within-cell tie. Let $U$ be the pooled midrank of candidate statistic $X$ and $V$ the pooled midrank of $T$. Since $T$ is constant within $g$, $V=v_g$ almost surely conditional on $G=g$. Hence
\[
\operatorname{Cov}(U,V\mid G=g)
=\operatorname{Cov}(U,v_g\mid G=g)=0.
\]
The law of total covariance gives
\begin{align*}
\operatorname{Cov}(U,V)
&=\mathbb E[\operatorname{Cov}(U,V\mid G)]
+\operatorname{Cov}(\mathbb E[U\mid G],\mathbb E[V\mid G])\\
&=\operatorname{Cov}(\mathbb E[U\mid G],v_G).
\end{align*}
Spearman correlation is Pearson correlation of midranks, so its covariance numerator contains only covariance between cell-average candidate ranks and cell target ranks. Candidate replication can change cell weights, marginal rank scaling, and the nominal number of rows, but it cannot add target information that distinguishes candidates inside a cell. If every cell has the same target, $V$ has zero variance and the pooled Spearman correlation is undefined.

\section{Experimental design and provenance}
\label{app:design}

\paragraph{Phase 1.}
Each model--layer--task cell uses the same extracted representations, target labels, control labels, fixed target/control evaluators, and five interventions for all 60 candidates. The candidate probes differ by architecture and seed. The audited target is the maximum in Eq.~\ref{eq:phase1}. The original prediction used raw mean absolute cosine between flattened probe parameters and the top 20 Hessian eigenvectors. That statistic changes with parameter dimension and with the basis chosen inside repeated eigenspaces, so the failed re-test cannot answer a basis-invariant geometry question.

\paragraph{Matched-versus-split control.}
For each of six models, we use 5 prespecified sampled layers for SVA and SST-2. Five seeded attacker/evaluator pairs train on separate sentence groups. Each attacker generates one edit that is hashed and scored unchanged by its matched and split evaluators, yielding 300 pair units in 60 cells nested in 12 model--task blocks. The locked denominator floor excludes both conditions for 11 affected pairs from numerical summaries (289 analyzed pairs and 59 analyzable cells) while retaining every requested row and reason in the artifact. We average valid pairs within cells and layers within equally weighted blocks. The primary 95\% interval hierarchically resamples model--task blocks, layers, and pairs; the primary exact sign-flip test flips the nonzero block contrasts.

\paragraph{Candidate diagnostic.}
A four-way sentence-disjoint split assigns 40\% of examples to candidate fitting, 20\% to evaluator fitting, 30\% to direction estimation and scoring, and 10\% to candidate testing. A model--task run must pass middle-layer target/control learnability and shuffled-label gates. Each layer then requires five bagged target/control evaluators above $0.60$ out-of-bag accuracy; denominators must exceed a $0.55$ floor. The run yields 3,360 candidate records from 56 of 90 possible cells. Fifteen cells fail model--task gates and 19 fail layer-evaluator gates. Fourteen of the latter occur at the first two sampled layers. Only Pythia and GPT-2 contribute WinoGender cells.

\paragraph{Synthetic validation.}
The generator draws orthonormal target, control, and shortcut axes in $\mathbb R^{256}$. It varies the probability that shortcut and target labels agree during candidate training, then evaluates with independent target and shortcut labels. Linear ground truth is absolute cosine with the planted target axis. The held-out nonlinear family uses mean principal-angle cosine with a planted two-dimensional XOR subspace. The delivered rows omit one registered correlated target--shortcut condition. The main text reports both pooled and within-replicate/shortcut associations. The registered battery requires all rank, range, dissociation, random-control, and saturation checks; the nonlinear saturation failure fixes the overall outcome.

\section{Additional audit results}

\begin{table}[ht]
\centering
\small
\begin{tabular}{lccccc}
\toprule
 & \multicolumn{2}{c}{target damage} & \multicolumn{2}{c}{control preservation} & first max \\
Method & mean & median & mean & median & (\%) \\
\midrule
INLP     & 0.35 & 0.00 & 0.96 & 1.00 & 11.1 \\
RLACE    & 0.08 & 0.00 & 0.98 & 1.00 & 3.3 \\
AlterRep & 0.95 & 1.00 & 0.98 & 1.00 & 78.9 \\
FGSM     & 1.00 & 1.00 & 0.69 & 0.83 & 6.7 \\
PGD      & 1.00 & 1.00 & 0.69 & 0.83 & 0.0 \\
\bottomrule
\end{tabular}
\caption{Phase-1 method decomposition over 5,400 records. ``First max'' follows the implementation's list-order tie rule, which gives PGD no credit when it ties the earlier FGSM value.}
\label{tab:methods-app}
\end{table}

The post-registration Phase-2 re-test is descriptive because the synthetic battery rejects $\Dcand$. Across 168 architecture medians in 56 cells, $\rho(A,\Dcand)=-0.042$ with archived permutation $p=.59$. The registered cell-bootstrap 95\% interval is $[-0.228,0.153]$. Post-hoc intervals are $[-0.296,0.268]$ under a 14-block model--task bootstrap, $[-0.245,0.109]$ under a six-model bootstrap, and $[-0.346,0.278]$ under a model/task/layer hierarchical bootstrap. Within-cell architecture ranks give $\rho=-0.143$, interval $[-0.320,0.041]$. The architecture hit rate is $0.375$ (21/56), interval $[0.241,0.509]$.

% BEGIN POST-RUN CONSTRUCT-PILOT-APPENDIX UPDATE
\section{Disclosed pilot compatibility checks}
\label{app:pilot-compatibility}
In the disclosed \ConstructCell{} pilot, orientation calibration changed median fixed-evaluator damage from \ConstructRawMedian{} to \ConstructOrientedMedian{} across \ConstructCandidates{} candidate edits. Fresh linear decoders reached median final-test accuracy \ConstructFreshLinear{} on edited states versus their \ConstructFreshLinearBaseline{} unedited baseline; fresh MLP decoders reached \ConstructFreshMLP{} versus \ConstructFreshMLPBaseline{}. These endpoints were inspected before the prospective panel was registered, receive no confirmatory p-value or decision, and do not establish erasure. Orientation was selected only on the disjoint calibration split and frozen for final-test evaluation.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.9\linewidth]{figures/fig_orientation_redecodability.pdf}
  \caption{Descriptive pilot compatibility endpoints for the 60 candidate-conditioned edits in Qwen2.5-1.5B/SST-2/layer 14. Panels align raw fixed-evaluator damage, calibration-frozen orientation damage, and fresh linear/MLP target-label accuracy.}
  \label{fig:orientation-redecodability}
\end{figure}
% END POST-RUN CONSTRUCT-PILOT-APPENDIX UPDATE

\section{Reproducibility and assets}
The anonymous code supplement is available at
\href{https://anonymous.4open.science/r/Neurips-Code-Supplement-DE78}
{this anonymous repository}.
It contains source code, tests, locked specifications, registrations, environment-reconstruction manifests, and exact commands that regenerate the analyses, figures, and manuscript. Generated rows, checkpoints, figures, caches, and manuscript builds are intentionally excluded from Git; each run writes immutable, hash-verified artifacts to a timestamped local directory. The code is released under the MIT License. Model weights are not redistributed.

The submission run used one NVIDIA GeForce RTX~5070 GPU (12,227 MiB), an Intel Core i9-14900K CPU (32 logical cores), and 47.8 GiB RAM on Windows 11, with Python 3.13.5, PyTorch 2.12.0.dev20260408+cu128 (CUDA 12.8), and Transformers 5.7.0. Transformer extraction and the registered candidate MLP/Jacobian operations use the GPU; probes, edits, fresh decoders, bootstraps, and exact tests run on CPU. The run manifest records stage-level wall time, peak process memory, device selection, and storage use.

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{p{0.24\linewidth}p{0.28\linewidth}p{0.38\linewidth}}
\toprule
Asset & Revision & License or terms \\
\midrule
Pythia-160M & \texttt{50f5173d9} & Apache-2.0; \url{https://huggingface.co/EleutherAI/pythia-160m} \\
GPT-2 & \texttt{607a30d78} & MIT; \url{https://huggingface.co/openai-community/gpt2} \\
BERT base uncased & \texttt{86b5e0934} & Apache-2.0; \url{https://huggingface.co/google-bert/bert-base-uncased} \\
Qwen2.5-1.5B & \texttt{8faed761d} & Apache-2.0; \url{https://huggingface.co/Qwen/Qwen2.5-1.5B} \\
Gemma-2-2B & \texttt{c5ebcd40d} & Gemma Terms; gated access; \url{https://huggingface.co/google/gemma-2-2b} \\
Llama-3.2-3B & \texttt{13afe5124} & Llama 3.2 Community License and acceptable-use policy; gated access; \url{https://huggingface.co/meta-llama/Llama-3.2-3B} \\
SST-2 & \texttt{8d51e7e48} & Hugging Face card reports license unspecified; \url{https://huggingface.co/datasets/stanfordnlp/sst2} \\
SVA dependencies & repository snapshot & MIT; \url{https://github.com/TalLinzen/rnn_agreement} \\
WinoGender schemas & repository snapshot & MIT; \url{https://github.com/rudinger/winogender-schemas} \\
\bottomrule
\end{tabular}
\caption{External assets, exact cached revisions where applicable, and governing terms. Dataset and model creators are cited in the main text.}
\label{tab:assets}
\end{table}

\paragraph{Tool assistance.}
Large-language-model tools supported code implementation but were not part of the evaluated method. The authors reviewed the code, recomputed reported analyses, and retain responsibility for the submission.

\clearpage
\input{neurips_2026_checklist.tex}

\end{document}
